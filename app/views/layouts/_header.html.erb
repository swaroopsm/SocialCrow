<div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <% if signed_in? %>
          <%= link_to "SocialCrow", "/@/#{current_user.username}", :class => "brand" %>
         	<% else %>
         		<a class="brand socialcrow-font" href="/">Social Crow</a>
         	<% end %>
          <ul class="nav  pull-right">
          	<% if signed_in? %>
          		<li class="dropdown" id="menu1">
							<a class="dropdown-toggle" data-toggle="dropdown" href="#menu1">
								<%= current_user.name %>
								<b class="caret"></b>
							</a>
							<ul class="dropdown-menu">
								<li>
									<%= link_to "/@/#{current_user.username}/profile" do %>
										Profile
									<% end %>
									</li>
								<li><%= link_to "Settings","/@/#{current_user.username}/profile/settings" %></li>
								<li class="divider"></li>
								<li><%= link_to "Logout","/logout" %></li>
							</ul>
						</li>
          	<% else %>
          		<li><h5><a href="/login"><button class="btn btn-info">Login</button></a></h5></li>
          	<% end %>
          </ul>
        </div>
      </div>
    </div>
